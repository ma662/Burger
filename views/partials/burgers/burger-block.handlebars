<li>
	{{burger_name}}

	<button class="change-state" data-id="{{id}}" data-newstate="{{eaten}}">
		{{#if eaten}}EAT BURGER!{{else}}VOMIT BURGER!{{/if}}
	</button>

	{{!-- <button class="delete-cat" data-id="{{this.id}}">DELETE!</button> --}}
</li>
